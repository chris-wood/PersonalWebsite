---
title: Christopher A. Wood | Research
---

<h2> Publications, Technical Reports, Internet Drafts</h2>

<p>See my <a href="https://scholar.google.com/citations?user=f_QuauMAAAAJ&hl=en">Google Scholar</a> profile
for a complete collection of my publications.</p>

<h3><b>Conference Proceedings</b></h3>

<ol>
    <% data.publications.pubs.each do |key, p| %>
    <% if p.type == "conference" then %>
    <li>
        <p><b><%= link_to p.title, "publications/#{key}.html" %></b> <% if p.awards then %><span class="tag is-danger"><%= p.awards %></span><% end %></br>
        <%= p.authors %><br/>
        <% if p.links["pdf"] then %><a class="button is-small is-info" href="<%= p.links["pdf"] %>">PDF</a><% end %>
        <% if p.links["slides"] then %><a class="button is-small is-success" href="<%= p.links["slides"] %>">slides</a><% end %>
        <% if p.links["code"] then %><a class="button is-small is-warning" href="<%= p.links["code"] %>">code</a><% end %>
        </p>
    </li>
    <% end %>
    <% end %>
</ol>

<h3>Journal Articles</h3>

<ol>
    <% data.publications.pubs.each do |key, p| %>
    <% if p.type == "journal" then %>
    <li>
        <p><b><%= link_to p.title, p.page %></b><br/>
        <%= p.authors %></br>
        <% if p.links["pdf"] then %><a class="button is-small is-info" href="<%= p.links["pdf"] %>">PDF</a><% end %>
        <% if p.links["slides"] then %><a class="button is-small is-success" href="<%= p.links["slides"] %>">slides</a><% end %>
        </p>
    </li>
    <% end %>
    <% end %>
</ol>

<h3>Posters</h3>
<ol>
    <% data.publications.pubs.each do |key, p| %>
    <% if p.type == "poster" then %>
    <li>
        <p><b><%= link_to p.title, p.page %></b><br/>
        <%= p.authors %></br>
        <% if p.links["pdf"] then %><a class="button is-small is-info" href="<%= p.links["pdf"] %>">PDF</a><% end %>
        <% if p.links["slides"] then %><a class="button is-small is-success" href="<%= p.links["slides"] %>">slides</a><% end %>
        </p>
    </li>
    <% end %>
    <% end %>
</ol>

<h3>Theses</h3>
<ol>
    <% data.publications.pubs.each do |key, p| %>
    <% if p.type == "thesis" then %>
    <li>
        <p><b><%= link_to p.title, p.page %></b><br/>
        <%= p.authors %></br>
        <% if p.links["pdf"] then %><a class="button is-small is-info" href="<%= p.links["pdf"] %>">PDF</a><% end %>
        <% if p.links["slides"] then %><a class="button is-small is-success" href="<%= p.links["slides"] %>">slides</a><% end %>
        </p>
    </li>
    <% end %>
    <% end %>
</ol>

<h3>Technical Reports</h3>
<ol>
    <% data.publications.pubs.each do |key, p| %>
    <% if p.type == "report" then %>
    <li>
        <p><b><%= link_to p.title, p.page %></b><br/>
        <%= p.authors %></br>
        <% if p.links["pdf"] then %><a class="button is-small is-info" href="<%= p.links["pdf"] %>">PDF</a><% end %>
        </p>
    </li>
    <% end %>
    <% end %>
</ol>

<h3>Internet Drafts</h3>
<ol>
    <% data.publications.pubs.each do |key, p| %>
    <% if p.type == "id" then %>
    <li>
        <p><b><%= link_to p.title, p.page %></b><br/>
        <%= p.authors %></br>
        <a class="button is-small is-info" href="<%=p.links["html"]%>">HTML</a>
        <a class="button is-small is-success" href="<%=p.links["txt"]%>">TXT</a>
        </p>
    </li>
    <% end %>
    <% end %>
</ol>

<h2>Talks and Presentations</h2>
<ol>
    <% data.talks.talks.each do |t| %>
    <li>
        <p>
        <% if t.links["pdf"] and t.links["pdf"].length > 0 then %>
        "<%= link_to t.title, t.links["pdf"] %>,"</b> <%= t.venue %>.
        <% elsif t.links["aux"] and t.links["aux"].length > 0 then %>
        <b>"<%= link_to t.title, t.links["aux"] %>,"</b> <%= t.venue %>.
        <% else %>
        <b>"<%= t.title %>,"</b> <%= t.venue %>.
        <% end %>
        </p>
        <!-- Links to references -->
        <% if t.references and t.references.length > 0 then %>
        <ul>
            <% t.references.each do |l| %>
                <li>"<%= link_to l.name, l.link %>"</li>
            <% end %>
        </ul>
        <% end %>
    </li>
    <% end %>
</ol>

<h2>Patents</h2>
<ol>
    <% data.patents.each do |p| %>
    <li>
        <% if p.url and p.url.length > 0 then %> <p><b>"<%= link_to p.title, p.url %>,"</b> <%= p.authors %>, <%= p.details %>.</p>
        <% else %>
        <p><b>"<%= p.title %>,"</b> <%= p.authors %>, <%= p.details %>.</p>
        <% end %>
    </li>
    <% end %>
</ol>
